{% extends 'base.html.twig' %}

{% block title %}Premade Finder{% endblock %}

{% block body %}
{{ include('./admin/header.html.twig')}}

<section id ="adminIndex">
    <nav>
        <ul class="enlacesAdmin">
        {% if app.user %}
            <a href="{{ path('app_grupo_index', {'id': app.user.id}) }}"><li>Grupos League Of Leguends</li></a>
            <a href="{{ path('app_grupo_index2', {'id': app.user.id}) }}"><li>Grupos Valorant</li></a>
            {% else %}
            <a href="{{ path('user_new') }}"><li>Registrese en nuestra plataforma</li></a>
            <a href="/login"><li>Login</li></a>
            <a href = "/admin/info">¿Quiénes somos?</a>
        {% endif %}
        </ul>
    </nav>
</section>

{{ include('./admin/footer.html.twig')}}
{% endblock %}
